import express, { Request, Response } from "express";
import { pool } from "..";

const router = express.Router();
// update name

// update email

// update password

// delete acoount

export default router;
